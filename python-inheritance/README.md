# Python - Inheritance

This project introduces inheritance in Python and explores how classes inherit attributes and methods from other classes. It covers parent and child classes, method overriding, multiple inheritance, and the use of built-in functions such as `isinstance()`, `issubclass()`, `type()`, and `super()`. The project also reinforces writing clean, well-documented, and PEP 8-compliant Python code.
