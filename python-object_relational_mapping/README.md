# Python - Object-relational Mapping

This project demonstrates how to connect Python applications to a MySQL database using both **MySQLdb** and **SQLAlchemy**. It covers executing SQL queries, retrieving and modifying data, and using Object-Relational Mapping (ORM) to interact with database tables through Python objects instead of writing raw SQL.

