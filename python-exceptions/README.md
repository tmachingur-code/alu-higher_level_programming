# Python - Exceptions

This project introduces exception handling in Python. It covers the difference between errors and exceptions, how to use `try`, `except`, `else`, and `finally` blocks, how to raise built-in exceptions, and how to write programs that handle runtime errors gracefully. The exercises focus on building robust Python code while following PEP 8 coding standards.
